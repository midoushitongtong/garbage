import PageContent from './pageContent';

export const generateMetadata = () => {
  return {
    title: 'Play - Hello World',
  };
};

const Page = () => {
  return <PageContent />;
};

export default Page;
