import PageContent from './pageContent';

export const generateMetadata = () => {
  return {
    title: 'Hello World',
  };
};

const Page = () => {
  return <PageContent />;
};

export default Page;
